# Turning Shop

Клиент-серверное веб-приложение магазина Turning Shop.
Работает полностью в браузере: HTML/CSS/JavaScript + SQLite (sql.js/WebAssembly).

## Функциональность

### Авторизация
- Вход по логину из предопределённого списка (без пароля).
- ФИО пользователя и его роль отображаются в правом верхнем углу.
- Роли: **Клиент**, **Менеджер**, **Администратор**.

Демо-логины:

| Логин | Роль |
|---|---|
| `yuder`, `kiashir`, `ever`, `hinn`, `finn` | Клиент |
| `manager` | Менеджер |
| `admin` | Администратор |

### Каталог товаров
- Список товаров из БД с ценами, количеством и бейджами «много/мало».
- Автоматический расчёт цены со скидкой (5% или 10% в зависимости от остатка).
- Поиск по названию, фильтрация по наличию, сортировка по 8 параметрам.

### Карточка товара
- Изображение, наименование, цена, скидка, доступное количество.
- Выбор размера (из таблицы `Product_Sizes`).
- Ввод количества с валидацией остатка.

### Корзина
- Добавление товаров из каталога и карточки.
- Изменение количества, удаление позиций, подсчёт итога.
- Оформление заказа с фиксацией даты.

### Заказы
- **Клиент** видит только свои заказы.
- **Менеджер** и **Администратор** видят все заказы.
- Просмотр состава заказа: модель, размер, количество, цена за единицу, сумма.
- **Менеджер** может добавлять и удалять позиции.
- **Администратор** может дополнительно менять дату заказа.

### Обработка ошибок
- Модальные окна с иконками (ℹ️ / ⚠️ / ⛔) и заголовками.
- Валидация ввода количества и даты.
- Проверка остатка товара при оформлении.

## Структура проекта
/
├── index.html — экран входа
├── catalog.html — каталог товаров
├── product.html — карточка товара
├── cart.html — корзина
├── orders.html — список заказов
├── style.css — общие стили
├── db.js — общий модуль (БД, store, утилиты)
├── app-login.js
├── app-catalog.js
├── app-product.js
├── app-cart.js
├── app-orders.js
├── db.db — SQLite-база (таблицы Products, Customers,
│ Orders, Order_Details, Product_Sizes)
├── sql-wasm.js — библиотека sql.js
├── sql-wasm.wasm — WebAssembly-движок SQLite
└── logo.svg — логотип приложения

## Схема БД

- **Products** — товары (код, наименование, цена, количество).
- **Customers** — клиенты (код, ФИО, дата рождения, адрес, телефон).
- **Orders** — заказы (код, код клиента, дата).
- **Order_Details** — состав заказов (код заказа, код товара, количество).
- **Product_Sizes** — размеры товаров.

## Запуск

1. Склонировать репозиторий.
2. Открыть `index.html` через локальный веб-сервер (`python -m http.server 8000`)
   или развернуть на GitHub Pages.
3. Войти под одним из логинов и работать с каталогом/корзиной/заказами.

## Технологии

- HTML5, CSS3 (БЭМ-подобные классы)
- Vanilla JavaScript (ES2017+)
- sql.js — SQLite, скомпилированный в WebAssembly
- localStorage — хранение сессии, корзины и заказов
